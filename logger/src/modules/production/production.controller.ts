import type { NextFunction, Request, Response } from "express";
import { buildAuthenticatedAuditContext } from "../../shared/http/audit-context.js";
import { catalogInputSchema, administrativeCatalogInputSchema, catalogUpdateSchema, idParamsSchema, listSchema, customDefinitionSchema, customDefinitionUpdateSchema, customValueSchema, orderListSchema, productionOrderCreateSchema, transformationOrderCreateSchema, batchListSchema, containerCreateSchema, containerUpdateSchema, containerAssignSchema, containerTransferSchema, containerPartialTransferSchema, sourceContainerParamsSchema, workListSchema, workCreateSchema, workCorrectionSchema, workInputSchema, workInputReversalSchema, grapeReceptionSchema, measurementCreateSchema, measurementListSchema, transformationCreateSchema, transformationListSchema, releaseBatchSchema, reverseReleaseSchema, measurementCorrectionSchema, receptionCorrectionSchema } from "./production.schema.js";
import type { CatalogKind, ReadCatalogKind } from "./production.dto.js";
import type { ProductionService } from "./production.service.js";
import { AppError } from "../../shared/errors/app-error.js";
export class ProductionController {
  constructor(private readonly service: ProductionService, private readonly kind: ReadCatalogKind) {}
  list = async (req: Request, res: Response, next: NextFunction) => { try { const q = listSchema.parse(req.query); const filters = { page: q.page, pageSize: q.pageSize, ...(q.search !== undefined ? { search: q.search } : {}), ...(q.active !== undefined ? { active: q.active } : {}) }; const r = await this.service.list(this.kind, filters); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  create = async (req: Request, res: Response, next: NextFunction) => { try { const data = (this.kind === "participants" ? catalogInputSchema.parse(req.body) : administrativeCatalogInputSchema.parse(req.body)); const item = await this.service.create(this.kind as CatalogKind, data, buildAuthenticatedAuditContext(req, res)); res.status(201).json({ data: item }); } catch (e) { next(e); } };
  update = async (req: Request, res: Response, next: NextFunction) => { try { const parsed = catalogUpdateSchema.parse(req.body); const data = parsed.name !== undefined ? { name: parsed.name } : {}; const item = await this.service.update(this.kind as CatalogKind, idParamsSchema.parse(req.params).id, data, buildAuthenticatedAuditContext(req, res)); res.json({ data: item }); } catch (e) { next(e); } };
  activate = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.setActive(this.kind as CatalogKind, idParamsSchema.parse(req.params).id, true, buildAuthenticatedAuditContext(req, res)); res.json({ data: item }); } catch (e) { next(e); } };
  deactivate = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.setActive(this.kind as CatalogKind, idParamsSchema.parse(req.params).id, false, buildAuthenticatedAuditContext(req, res)); res.json({ data: item }); } catch (e) { next(e); } };
  definitions = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.listDefinitions({}) }); } catch (e) { next(e); } };
  createDefinition = async (req: Request, res: Response, next: NextFunction) => { try { const p = customDefinitionSchema.parse(req.body); const { options, ...base } = p; const data = options === undefined ? base : { ...base, options }; res.status(201).json({ data: await this.service.createDefinition(data, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  updateDefinition = async (req: Request, res: Response, next: NextFunction) => { try { const p = customDefinitionUpdateSchema.parse(req.body); const { options, ...base } = p; const data = options === undefined ? base : { ...base, options }; res.json({ data: await this.service.updateDefinition(idParamsSchema.parse(req.params).id, data, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  activateDefinition = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.setDefinitionActive(idParamsSchema.parse(req.params).id, true, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  deactivateDefinition = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.setDefinitionActive(idParamsSchema.parse(req.params).id, false, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  setValue = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.setValue(customValueSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listOrders = async (req: Request, res: Response, next: NextFunction) => { try { const q = orderListSchema.parse(req.query); const r = await this.service.listOrders(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getOrder = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.getOrder(idParamsSchema.parse(req.params).id); if (!item) return next(new AppError("PRODUCTION_ORDER_NOT_FOUND", "Production order not found", 404)); res.json({ data: item }); } catch (e) { next(e); } };
  createOrder = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createOrder(productionOrderCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  closeOrder = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.closeOrder(idParamsSchema.parse(req.params).id, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listTransformationOrders = async (req: Request, res: Response, next: NextFunction) => { try { const q = orderListSchema.parse(req.query); const r = await this.service.listTransformationOrders(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getTransformationOrder = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.getTransformationOrder(idParamsSchema.parse(req.params).id); if (!item) return next(new AppError("TRANSFORMATION_ORDER_NOT_FOUND", "Transformation order not found", 404)); res.json({ data: item }); } catch (e) { next(e); } };
  createTransformationOrder = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createTransformationOrder(transformationOrderCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  closeTransformationOrder = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.closeTransformationOrder(idParamsSchema.parse(req.params).id, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listBatches = async (req: Request, res: Response, next: NextFunction) => { try { const q = batchListSchema.parse(req.query); const r = await this.service.listBatches(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getBatch = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getBatch(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  getBatchTrace = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getBatchTrace(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  getBatchBalance = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getBatchBalance(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  releaseBatchToInventory = async (req: Request, res: Response, next: NextFunction) => { try { const body = releaseBatchSchema.parse(req.body); const { observations, ...required } = body; res.json({ data: await this.service.releaseBatchToInventory({ ...required, ...(observations === undefined ? {} : { observations }), productionBatchId: idParamsSchema.parse(req.params).id }, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  reverseRelease = async (req: Request, res: Response, next: NextFunction) => { try { const p = req.params as { batchId: string; releaseId: string }; res.json({ data: await this.service.reverseRelease(p.batchId, p.releaseId, reverseReleaseSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listReleases = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.listReleases(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  listInventoryReleaseWarehouses = async (_req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.listInventoryReleaseWarehouses() }); } catch (e) { next(e); } };
  listContainers = async (_req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.listContainers() }); } catch (e) { next(e); } };
  getContainer = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getContainer(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  getContainerOccupancies = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getContainerOccupancies(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  getContainerMovements = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getContainerMovements(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  createContainer = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createContainer(containerCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  updateContainer = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.updateContainer(idParamsSchema.parse(req.params).id, containerUpdateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  activateContainer = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.activateContainer(idParamsSchema.parse(req.params).id, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  deactivateContainer = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.deactivateContainer(idParamsSchema.parse(req.params).id, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  assignContainer = async (req: Request, res: Response, next: NextFunction) => { try { const id = idParamsSchema.parse(req.params).id; res.status(201).json({ data: await this.service.assignBatchToContainer({ ...containerAssignSchema.parse(req.body), destinationContainerId: id }, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  transferContainer = async (req: Request, res: Response, next: NextFunction) => { try { const sourceId = sourceContainerParamsSchema.parse(req.params).sourceId; res.json({ data: await this.service.transferBatchBetweenContainers({ ...containerTransferSchema.parse(req.body), sourceContainerId: sourceId }, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  partialTransferContainer = async (req: Request, res: Response, next: NextFunction) => { try { const sourceId = sourceContainerParamsSchema.parse(req.params).sourceId; res.json({ data: await this.service.transferBatchPartiallyBetweenContainers({ ...containerPartialTransferSchema.parse(req.body), sourceContainerId: sourceId }, buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listWorks = async (req: Request, res: Response, next: NextFunction) => { try { const q = workListSchema.parse(req.query); const result = await this.service.listWorks(q); res.json({ data: result.items, meta: result.pagination }); } catch (e) { next(e); } };
  getWork = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.getWork(idParamsSchema.parse(req.params).id) }); } catch (e) { next(e); } };
  createWork = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createWork(workCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  createWorkInput = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createWorkInput(idParamsSchema.parse(req.params).id, workInputSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  reverseWorkInput = async (req: Request, res: Response, next: NextFunction) => { try { const params = req.params as { workId: string; inputId: string }; res.json({ data: await this.service.reverseWorkInput(params.workId, params.inputId, workInputReversalSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  correctWork = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.correctWork(idParamsSchema.parse(req.params).id, workCorrectionSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listReceptions = async (req: Request, res: Response, next: NextFunction) => { try { const q = listSchema.parse(req.query); const r = await this.service.listReceptions(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getReception = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.getReception(idParamsSchema.parse(req.params).id); if (!item) return next(new AppError("GRAPE_RECEPTION_NOT_FOUND", "Grape reception not found", 404)); res.json({ data: item }); } catch (e) { next(e); } };
  createReception = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createReception(grapeReceptionSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listMeasurements = async (req: Request, res: Response, next: NextFunction) => { try { const q = measurementListSchema.parse(req.query); const r = await this.service.listMeasurements(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getMeasurement = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.getMeasurement(idParamsSchema.parse(req.params).id); if (!item) return next(new AppError("PRODUCTION_MEASUREMENT_NOT_FOUND", "Production measurement not found", 404)); res.json({ data: item }); } catch (e) { next(e); } };
  createMeasurement = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createMeasurement(measurementCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  correctMeasurement = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.correctMeasurement(idParamsSchema.parse(req.params).id, measurementCorrectionSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  correctReception = async (req: Request, res: Response, next: NextFunction) => { try { res.json({ data: await this.service.correctReception(idParamsSchema.parse(req.params).id, receptionCorrectionSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
  listTransformations = async (req: Request, res: Response, next: NextFunction) => { try { const q = transformationListSchema.parse(req.query); const r = await this.service.listTransformations(q); res.json({ data: r.items, meta: r.pagination }); } catch (e) { next(e); } };
  getTransformation = async (req: Request, res: Response, next: NextFunction) => { try { const item = await this.service.getTransformation(idParamsSchema.parse(req.params).id); if (!item) return next(new AppError("TRANSFORMATION_NOT_FOUND", "Transformation not found", 404)); res.json({ data: item }); } catch (e) { next(e); } };
  createTransformation = async (req: Request, res: Response, next: NextFunction) => { try { res.status(201).json({ data: await this.service.createTransformation(transformationCreateSchema.parse(req.body), buildAuthenticatedAuditContext(req, res)) }); } catch (e) { next(e); } };
}