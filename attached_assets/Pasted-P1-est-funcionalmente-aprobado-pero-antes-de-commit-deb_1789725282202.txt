P1 está funcionalmente aprobado, pero antes de commit debemos resolver
un único bloqueo técnico: procedencia y reproducibilidad del schema.

NO continúes con P2.
NO hagas commit.
NO hagas push.
NO modifiques datos productivos.

Actualmente:

- existe la migración:
  20260914000000_production_p1
- las tablas P1 existen en la base de desarrollo usada por tests;
- pero esa migración NO aparece registrada en _prisma_migrations.

Necesito determinar exactamente por qué.

FASE A — DIAGNÓSTICO DE DRIFT

Investiga y reporta:

1. Qué mecanismo creó actualmente las tablas P1 de la base de desarrollo:
   - ejecución anterior de migrate;
   - prisma db push;
   - setup de tests;
   - DDL manual;
   - otra causa.

2. Revisar configuración de tests y scripts relacionados con base de datos.
   Identificar si los tests crean/sincronizan schema automáticamente.

3. Comparar el schema real actual de las tablas P1 contra:
   - prisma/schema.prisma
   - migrations/20260914000000_production_p1/migration.sql

4. Reportar cualquier diferencia:
   - columnas;
   - tipos;
   - nullability;
   - defaults;
   - PK/FK;
   - unique constraints;
   - indexes;
   - enums.

No corregir silenciosamente diferencias.

FASE B — PRUEBA DE REPRODUCIBILIDAD

Sin utilizar la base de desarrollo actual como prueba suficiente,
validar que una base limpia puede construirse exclusivamente mediante
las migraciones versionadas del repositorio.

Usar una base/schema temporal o aislado apropiado.

Debe demostrarse:

estado anterior del proyecto
        +
migraciones versionadas
        ↓
schema final esperado incluyendo Production P1

No utilizar prisma db push como sustituto de esta prueba.

Después comprobar que:

- 20260914000000_production_p1 se aplica correctamente;
- Prisma Client genera correctamente;
- las constraints e índices esperados existen;
- la aplicación puede iniciar;
- los tests relevantes pueden ejecutarse contra el schema generado.

No conservar datos ni recursos temporales innecesarios después.

FASE C — DECISIÓN

Si la migración reproduce exactamente el schema:

- NO modificar la migración solo para hacer coincidir una base contaminada;
- reportar que el artifact versionado es reproducible;
- explicar qué causó que la base de desarrollo ya tuviera las tablas.

Si existe drift real:

DETENERTE.

Reportar:

- schema esperado;
- schema real;
- causa probable;
- propuesta de corrección.

No editar migration.sql ni schema.prisma hasta reportarlo.

VALIDACIÓN FINAL

Si no existe drift bloqueante ejecutar:

npm run db:validate
npm run db:generate
npm run typecheck
npm test
npm run build
git diff --check

Reportar finalmente:

1. causa exacta de las tablas P1 preexistentes;
2. estado de _prisma_migrations;
3. resultado de la prueba desde base limpia;
4. existencia o no de schema drift;
5. cualquier archivo modificado durante la investigación;
6. git status;
7. SHA actual;
8. resultados de validación;
9. confirmación de que P2 continúa sin iniciar.

No hacer commit.
No hacer push.